# Project AI Rules

## ドキュメント参照ルール
実装や設計変更を行う際は、必ず以下ディレクトリに含まれるドキュメントを参照してください。
また、アーキテクチャに依存する詳細なルールはアーキテクチャルールに記載されているので、実装や設計前に必ず確認してください。
- アーキテクチャルール： `docs/rules/`
- 仕様書: `docs/specs/`
- 設計書: `docs/design/`
- 報告書： `docs/reports/`

## ディレクトリ方針
- 全てのソースコードは`src/` 配下に配置すること。
- 全てのテストコードは`tests/`配下に配置すること。
- テストでファイルを作成する必要がある場合は、プログラミング言語が提供する（言語が提供できない場合はOSが提供する）自動クリーンアップ機能が働く一時ディレクトリを使用すること。

## 開発の進め方
- 開発は仕様書を基に設計書を`docs/design/`に作成すること。
- コーディングは作成した設計書を基に行うこと。
- 仕様書を基にしたコードの修正の指示があった場合は、仕様書と設計書の齟齬を解析し、仕様書を基に設計書を修正してから行うこと。
- 仕様書の確認がなく直接修正指示があった場合は、まず仕様書を修正し、修正した仕様書を基に設計書を修正してから行うこと。
- 不明な点や、仕様の矛盾や曖昧性を見つけた場合は、推測で進めずに必ず人間に質問すること。
- 事前にインストールされているもの以外で、設計・実装に必要なライブラリやパッケージが出てきたら人間に相談すること。
- テストを行ってソースを修正する場合にTryするのは最大3回まで。

## コミットについて
- コミットを指示されたら `git status` と `git diff` を確認し、以下のルールでコミットを実行してください。

1. **形式**: Conventional Commits 形式（日本語）
2. **Issue連携**: 現在のブランチ名に含まれる数字（Issue番号）を取得し、スコープに `#` 付きで含めること（例: `feat(#12): UIとメインロジックを実装`）

## 報告書について
- バグの発見や、調査・分析・検証等の報告書は、`docs/reports/`に作成すること。次に報告書の命名規則について記載します。

### レポート命名規則 (`docs/reports/`)
- ファイル名は以下の規則に従って作成・参照してください。
- フォーマット: `{prefix}-{Issue番号3桁}-{kebab-caseでの概要}.md`

### プレフィックスと例
- `bug-` : バグ報告書 (例: `bug-001-payment-failure.md`)
- `invest-` : 原因・障害調査 (例: `invest-001-payment-timeout.md`)
- `perf-` : パフォーマンス調査 (例: `perf-002-db-query-optimization.md`)
- `spike-` : 技術検証・事前調査 (例: `spike-005-pyside6-migration.md`)

### 注意点
- Issue番号は必ず3桁でゼロパディング（`001`, `012`, `105`）すること。
- Issue #12 の修正時は `*012*` に該当するファイルを優先して参照すること。

## やらないこと（Out of Scope）
- コードの整形はエディタのLinterやFormatterの機能に頼る為、整形のためにモデル等に問い合わせを行わないこと。
- 勝手にパッケージをインストールしないこと
- テストを勝手に行わないこと。
